import{Settings}from"../models/Settings.js";import{StorageService}from"../services/StorageService.js";import{BackupService}from"../services/BackupService.js";import{money,nav,registerPWA}from"../utils/ui.js";const storage=new StorageService,backup=new BackupService(storage);function render(msg=""){const s=storage.getSettings();document.querySelector("#app").innerHTML=`<header class="top"><div><h1>הגדרות</h1><div class="sub">מחירים, גיבוי ושחזור</div></div></header>${msg?`<div class="notice">${msg}</div>`:""}<section class="card"><div class="field"><label>מחיר חפיסה ₪</label><input id="price" type="number" min="0" step=".01" value="${s.packPrice}"></div><div class="field"><label>מספר סיגריות בחפיסה</label><input id="qty" type="number" min="1" step="1" value="${s.cigarettesPerPack}"></div><div class="row"><span>מחיר מחושב לסיגריה</span><strong>${money(s.cigarettePrice)}</strong></div><button id="save" class="add">שמירת הגדרות</button></section><h2 class="section-title">גיבוי מקומי</h2><section class="card"><div class="row"><span>ייצוא כל הנתונים ל-JSON</span><button id="export" class="btn">ייצוא</button></div><div class="row"><span>שחזור מקובץ JSON</span><label class="btn">בחירת קובץ<input id="import" type="file" accept=".json,application/json" hidden></label></div></section><h2 class="section-title">איפוס</h2><section class="card"><button id="clear" class="btn danger">מחיקת כל הנתונים</button></section>${nav("settings")}`;document.querySelector("#save").onclick=()=>{storage.saveSettings(new Settings(document.querySelector("#price").value,document.querySelector("#qty").value));render("ההגדרות נשמרו.")};document.querySelector("#export").onclick=()=>backup.download();document.querySelector("#import").onchange=async e=>{try{await backup.restore(e.target.files[0]);render("הגיבוי שוחזר בהצלחה.")}catch(err){alert(err.message)}};document.querySelector("#clear").onclick=()=>{if(confirm("למחוק לצמיתות את כל הנתונים במכשיר?")){storage.clear();render("כל הנתונים נמחקו.")}}}registerPWA();render();